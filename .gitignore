node_modules/

# Build output — regenerate with `npm run build`
dist/

# Original upstream build, extracted from rentvine-mcp-1.3.2.tgz for diffing.
# Recreate with: tar -xzf rentvine-mcp-1.3.2.tgz && mv package reference/dist-1.3.2-original
reference/

# Credentials
.env
.env.*
!.env.example

# Deployment launcher — holds live API keys and MCP_AUTH_TOKEN.
# Template is start-mcp.sh.example; the filled-in copy must never be committed.
start-mcp.sh
ecosystem.config.cjs

# pm2 local state
.pm2/

# Editor / OS
.DS_Store
*.swp
.idea/
